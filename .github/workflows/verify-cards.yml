name: Verify credit card offers

# Weekly verification of every non-expired card in the catalog.
# Hits each issuer's offer page, compares to stored bonus/spend/fee,
# writes problem rows to Supabase.card_verifications. Admin reviews the
# queue at /admin (Card Verifications tab).
#
# Required GitHub repo secrets:
#   - NEXT_PUBLIC_SUPABASE_URL
#   - SUPABASE_SERVICE_ROLE_KEY

on:
  schedule:
    # Sundays 14:00 UTC (Sunday morning Hawaii / midday US Eastern).
    - cron: "0 14 * * 0"
  workflow_dispatch: {}

jobs:
  verify:
    runs-on: ubuntu-latest
    # First run took 30+ min on a full uncached fetch (Amex CSP fallback +
    # IHG/issuer retries push real fetch latency to 10-30s/page). Give it
    # plenty of headroom — the cron runs once a week, runtime cost is fine.
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run verify:cards
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        # Drop --no-cache: each Actions run starts with an empty .cache
        # directory anyway, so it's a no-op that just signals intent.
        run: npm run verify:cards -- --persist

      - name: Upload report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verification-output
          path: verification-output/
          retention-days: 30
